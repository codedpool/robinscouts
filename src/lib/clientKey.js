// sessionStorage key for a visitor's own Bright Data API key. Client-only —
// never sent anywhere but our own API routes, never persisted server-side,
// cleared automatically when the tab closes.
export const API_KEY_STORAGE = "rs_bd_api_key";
